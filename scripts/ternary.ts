/* Перетворити цю умову, використовуючи тернарний оператор.
let isNightTime = true;

if (isNightTime) {
    console.log('Turn on the lights!');
} else {
    console.log('Turn off the lights!');
} */

let isNightTime = true;

console.log(isNightTime ? 'Turn on the lights!' : 'Turn off the lights!');